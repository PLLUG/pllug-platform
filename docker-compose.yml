version: '2'
services:
  rabbitmq: 
    image: rabbitmq:management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"
  api:
    build: ./api
    ports:
      - "8080:8080"
  someservice:
    build: ./some-service